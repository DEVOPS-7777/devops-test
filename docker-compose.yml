version: "3.9"

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      REDIS_URL: redis://redis:6379
      APP_SECRET: ${APP_SECRET}
    depends_on:
      - redis

  redis:
    image: redis:7-alpine